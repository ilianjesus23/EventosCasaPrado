// Copy this file to env.js and fill in your Supabase credentials.
// Get them from: https://supabase.com/dashboard → your project → Settings → API
//
// env.js is listed in .gitignore so your keys are never committed.
// On Vercel, set these as Environment Variables in the project settings instead.

window.ENV = {
  SUPABASE_URL: 'https://YOUR_PROJECT_ID.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR_ANON_PUBLIC_KEY',
};
